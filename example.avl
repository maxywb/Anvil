fun foo(in1,in1){
return 13,14;
};

{ 
  # you can't mix function declarations and executable code in the same scope
  # work around this by putting executable code in sub-scope

  x = 12;

  y = { 
  field1 = 15; 
  field2 = 16;
  subfun = fun (in){
    foo(in,in+1);
  };
  };
  
  w,z = foo(17,18);
  
  array= 1:10;
  
  x = fun (in){ 
    return in+1;
  };
  
  x(15);
}
